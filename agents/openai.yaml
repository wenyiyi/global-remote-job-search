interface:
  display_name: "Global Remote Job Search"
  short_description: "Find overseas backend roles remote from China"
  default_prompt: "Use $global-remote-job-search to run today’s remote backend job search in dry-run mode."
